#include "gles2_backend.h"

/* Default dimensions used by the HUD's legacy non-menu branch.
 * The menu branch has its own correctly scoped width/height values. */
float w = 1280.0f;
float h = 720.0f;
